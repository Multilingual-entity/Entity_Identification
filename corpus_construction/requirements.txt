# Corpus construction only. The experiment itself needs pipeline/requirements_a100.txt.
requests>=2.31
pandas>=2.1,<3
numpy>=1.26,<3
scipy>=1.11,<2

# Romanization, used for review sheets and for the transliteration audit. Every import
# of these is guarded, so the scripts run without them and fall back to weaker
# normalisation; install them to reproduce the reported numbers.
unidecode>=1.3
anyascii>=0.3
indic-transliteration>=2.3
aksharamukha>=2.1
